import React from "react";
import type { SlideshowProps } from "./types";
declare function Slideshow({ slideDuration, // 5 seconds
images, arrowConfig, selectorConfig, textConfig, imageConfig, }: SlideshowProps): React.JSX.Element;
export default Slideshow;
